name: lukehog
description: Dead simple analytics for any Flutter environment
version: 0.4.5
repository: https://github.com/lukepighetti/lukehog
homepage: https://lukehog.com

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  lukehog_client: ^0.4.5
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
